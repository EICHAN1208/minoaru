<div class="">
  <%= form_tag("/kakomons/#{@kakomon.id}", method: :patch ) do %>
    <h3>
      編集する
    </h3>
    <input type="string" name="prefecture" value="<%= @kakomon.prefecture %>" placeholder="都道府県">
    <textarea name="text" placeholder="text" rows="10" cols="30"><%= @kakomon.text %></textarea>
    <input type="submit" value="SENT">
  <% end %>
</div>